{
  "_": "GENERATED by `cargo run -p noeta-pm --example semver_vectors` — do not edit. Every `expected` is `semver::VersionReq::matches` itself; `affects` is what `Advisory::affects` (and so `noeta audit`) returns, which collapses unknown to false.",
  "cases": [
    { "rule": "caret: ^1.2.3 := >=1.2.3, <2.0.0", "version": "1.2.3", "req": "^1.2.3", "expected": true, "affects": true },
    { "rule": "caret: ^1.2.3 := >=1.2.3, <2.0.0", "version": "1.2.4", "req": "^1.2.3", "expected": true, "affects": true },
    { "rule": "caret: ^1.2.3 := >=1.2.3, <2.0.0", "version": "1.9.9", "req": "^1.2.3", "expected": true, "affects": true },
    { "rule": "caret: ^1.2.3 := >=1.2.3, <2.0.0", "version": "1.2.2", "req": "^1.2.3", "expected": false, "affects": false },
    { "rule": "caret: ^1.2.3 := >=1.2.3, <2.0.0", "version": "2.0.0", "req": "^1.2.3", "expected": false, "affects": false },
    { "rule": "caret: ^1.2.3 := >=1.2.3, <2.0.0", "version": "1.2.3", "req": "1.2.3", "expected": true, "affects": true },
    { "rule": "caret: ^1.2.3 := >=1.2.3, <2.0.0", "version": "1.9.0", "req": "1.2.3", "expected": true, "affects": true },
    { "rule": "caret: ^1.2.3 := >=1.2.3, <2.0.0", "version": "2.0.0", "req": "1.2.3", "expected": false, "affects": false },
    { "rule": "caret: the minor is the breaking axis below 1.0", "version": "0.2.3", "req": "^0.2.3", "expected": true, "affects": true },
    { "rule": "caret: the minor is the breaking axis below 1.0", "version": "0.2.9", "req": "^0.2.3", "expected": true, "affects": true },
    { "rule": "caret: the minor is the breaking axis below 1.0", "version": "0.3.0", "req": "^0.2.3", "expected": false, "affects": false },
    { "rule": "caret: the minor is the breaking axis below 1.0", "version": "0.2.2", "req": "^0.2.3", "expected": false, "affects": false },
    { "rule": "caret: the minor is the breaking axis below 1.0", "version": "0.0.3", "req": "^0.0.3", "expected": true, "affects": true },
    { "rule": "caret: the minor is the breaking axis below 1.0", "version": "0.0.4", "req": "^0.0.3", "expected": false, "affects": false },
    { "rule": "caret: the minor is the breaking axis below 1.0", "version": "0.0.2", "req": "^0.0.3", "expected": false, "affects": false },
    { "rule": "caret: a partial requirement widens", "version": "1.9.9", "req": "^1", "expected": true, "affects": true },
    { "rule": "caret: a partial requirement widens", "version": "2.0.0", "req": "^1", "expected": false, "affects": false },
    { "rule": "caret: a partial requirement widens", "version": "0.9.9", "req": "^0", "expected": true, "affects": true },
    { "rule": "caret: a partial requirement widens", "version": "1.0.0", "req": "^0", "expected": false, "affects": false },
    { "rule": "caret: a partial requirement widens", "version": "1.2.9", "req": "^1.2", "expected": true, "affects": true },
    { "rule": "caret: a partial requirement widens", "version": "1.3.0", "req": "^1.2", "expected": true, "affects": true },
    { "rule": "caret: a partial requirement widens", "version": "0.2.9", "req": "^0.2", "expected": true, "affects": true },
    { "rule": "caret: a partial requirement widens", "version": "0.3.0", "req": "^0.2", "expected": false, "affects": false },
    { "rule": "tilde", "version": "1.2.3", "req": "~1.2.3", "expected": true, "affects": true },
    { "rule": "tilde", "version": "1.2.9", "req": "~1.2.3", "expected": true, "affects": true },
    { "rule": "tilde", "version": "1.3.0", "req": "~1.2.3", "expected": false, "affects": false },
    { "rule": "tilde", "version": "1.2.2", "req": "~1.2.3", "expected": false, "affects": false },
    { "rule": "tilde", "version": "1.2.9", "req": "~1.2", "expected": true, "affects": true },
    { "rule": "tilde", "version": "1.3.0", "req": "~1.2", "expected": false, "affects": false },
    { "rule": "tilde", "version": "1.9.9", "req": "~1", "expected": true, "affects": true },
    { "rule": "tilde", "version": "2.0.0", "req": "~1", "expected": false, "affects": false },
    { "rule": "comparators", "version": "1.2.3", "req": "=1.2.3", "expected": true, "affects": true },
    { "rule": "comparators", "version": "1.2.4", "req": "=1.2.3", "expected": false, "affects": false },
    { "rule": "comparators", "version": "1.2.3", "req": "=1.2", "expected": true, "affects": true },
    { "rule": "comparators", "version": "1.9.9", "req": "=1", "expected": true, "affects": true },
    { "rule": "comparators", "version": "1.3.0", "req": ">1.2.3", "expected": true, "affects": true },
    { "rule": "comparators", "version": "1.2.3", "req": ">1.2.3", "expected": false, "affects": false },
    { "rule": "comparators", "version": "1.2.3", "req": ">=1.2.3", "expected": true, "affects": true },
    { "rule": "comparators", "version": "1.2.2", "req": ">=1.2.3", "expected": false, "affects": false },
    { "rule": "comparators", "version": "1.2.3", "req": "<1.2.4", "expected": true, "affects": true },
    { "rule": "comparators", "version": "1.2.4", "req": "<1.2.4", "expected": false, "affects": false },
    { "rule": "comparators", "version": "1.2.3", "req": "<=1.2.3", "expected": true, "affects": true },
    { "rule": "comparators", "version": "1.2.4", "req": "<=1.2.3", "expected": false, "affects": false },
    { "rule": "wildcards", "version": "1.5.0", "req": "1.*", "expected": true, "affects": true },
    { "rule": "wildcards", "version": "2.0.0", "req": "1.*", "expected": false, "affects": false },
    { "rule": "wildcards", "version": "1.2.9", "req": "1.2.*", "expected": true, "affects": true },
    { "rule": "wildcards", "version": "1.3.0", "req": "1.2.*", "expected": false, "affects": false },
    { "rule": "wildcards", "version": "1.0.0", "req": "*", "expected": true, "affects": true },
    { "rule": "wildcards", "version": "1.0.0", "req": "1.x", "expected": true, "affects": true },
    { "rule": "wildcards", "version": "1.0.0", "req": "1.X", "expected": true, "affects": true },
    { "rule": "the advisory fixture's range (NOETA-2026-0001)", "version": "1.0.0", "req": ">=1.0.0, <1.2.0", "expected": true, "affects": true },
    { "rule": "the advisory fixture's range (NOETA-2026-0001)", "version": "1.1.9", "req": ">=1.0.0, <1.2.0", "expected": true, "affects": true },
    { "rule": "the advisory fixture's range (NOETA-2026-0001)", "version": "1.2.0", "req": ">=1.0.0, <1.2.0", "expected": false, "affects": false },
    { "rule": "the advisory fixture's range (NOETA-2026-0001)", "version": "0.9.9", "req": ">=1.0.0, <1.2.0", "expected": false, "affects": false },
    { "rule": "pre-release: only a comparator that opted into the release line matches", "version": "1.2.4-alpha", "req": "^1.2.3", "expected": false, "affects": false },
    { "rule": "pre-release: only a comparator that opted into the release line matches", "version": "1.2.3-alpha", "req": "^1.2.3", "expected": false, "affects": false },
    { "rule": "pre-release: only a comparator that opted into the release line matches", "version": "1.2.3-beta", "req": ">=1.2.3-alpha, <1.3.0", "expected": true, "affects": true },
    { "rule": "pre-release: only a comparator that opted into the release line matches", "version": "1.2.3-alpha", "req": ">=1.2.3-alpha, <1.3.0", "expected": true, "affects": true },
    { "rule": "pre-release: only a comparator that opted into the release line matches", "version": "1.0.0-alpha", "req": "*", "expected": false, "affects": false },
    { "rule": "pre-release: only a comparator that opted into the release line matches", "version": "1.0.0", "req": "*", "expected": true, "affects": true },
    { "rule": "pre-release: only a comparator that opted into the release line matches", "version": "1.0.0-alpha", "req": ">=1.0.0-alpha", "expected": true, "affects": true },
    { "rule": "pre-release: SemVer §11 precedence", "version": "1.0.0", "req": ">1.0.0-alpha", "expected": true, "affects": true },
    { "rule": "pre-release: SemVer §11 precedence", "version": "1.0.0-alpha.1", "req": ">1.0.0-alpha.1, <1.0.1", "expected": false, "affects": false },
    { "rule": "pre-release: SemVer §11 precedence", "version": "1.0.0-alpha.2", "req": ">1.0.0-alpha.1, <1.0.1", "expected": true, "affects": true },
    { "rule": "pre-release: SemVer §11 precedence", "version": "1.0.0-alpha.beta", "req": ">1.0.0-alpha.1, <1.0.1", "expected": true, "affects": true },
    { "rule": "pre-release: SemVer §11 precedence", "version": "1.0.0-alpha", "req": ">1.0.0-alpha.1, <1.0.1", "expected": false, "affects": false },
    { "rule": "pre-release: SemVer §11 precedence", "version": "1.0.0-1", "req": ">1.0.0-alpha.1, <1.0.1", "expected": false, "affects": false },
    { "rule": "pre-release: SemVer §11 precedence", "version": "1.0.0-alpha.1.2", "req": ">1.0.0-alpha.1, <1.0.1", "expected": true, "affects": true },
    { "rule": "build metadata is accepted and ignored", "version": "1.2.3+build.5", "req": "^1.2.3", "expected": true, "affects": true },
    { "rule": "build metadata is accepted and ignored", "version": "1.2.3", "req": "^1.2.3+build.5", "expected": true, "affects": true },
    { "rule": "unparseable input is unknown, never a verdict", "version": "1.0.0", "req": "not a range", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "1.0.0", "req": ">=1.0.0,", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "1.0.0", "req": "1.*.3", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "not-a-version", "req": "^1", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "1.0", "req": "^1", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "1", "req": "^1", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "", "req": "^1", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "1.0.0", "req": "==", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "1.0.0", "req": "", "expected": null, "affects": false },
    { "rule": "unparseable input is unknown, never a verdict", "version": "1.0.0", "req": "   ", "expected": null, "affects": false },
    { "rule": "whitespace and multi-comparator forms", "version": "1.1.0", "req": ">=1.0.0 , <1.2.0", "expected": true, "affects": true },
    { "rule": "whitespace and multi-comparator forms", "version": "1.1.0", "req": ">=1.0.0,<1.2.0", "expected": true, "affects": true },
    { "rule": "whitespace and multi-comparator forms", "version": "1.1.0", "req": ">= 1.0.0, < 1.2.0", "expected": true, "affects": true },
    { "rule": "whitespace and multi-comparator forms", "version": "1.2.4", "req": "^1.2.3, ^1.2.4", "expected": true, "affects": true },
    { "rule": "whitespace and multi-comparator forms", "version": "1.2.3", "req": "^1.2.3, ^1.2.4", "expected": false, "affects": false }
  ]
}
